<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.refresh">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NamoScreen" android:theme="@style/namoTheme"></activity>
        <activity
            android:name=".PicsumScreen"
            android:theme="@style/picsumTheme" />
        <activity
            android:name=".QuotesScreen"
            android:theme="@style/quotesTheme" />
        <activity
            android:name=".TriviaScreen"
            android:theme="@style/triviaTheme" />
        <activity
            android:name=".BoredScreen"
            android:theme="@style/boredTheme" />
        <activity
            android:name=".JokesScreen"
            android:theme="@style/jokeTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>